'''_summary_

% performs MOD
** performs square
// performs integer divide

& performs binary AND
| performs binary OR
^ performs binary XOR (either but not both)
~ performs binary NOT
any returns True if any in a sequence are True
all returns True if all in a sequence are True
<< shift left by bits (1 << 3 = 001 -> 1000 or 8)
>> shift right by bits

is identity operator
in membership operator (contains)

True if 1=1 else False ternery operator

""" use
    for
    multiline
"""

\ escape character

x = [1,2,3,4,5]
x[0] = 1
x[-1] = 5
x[:2] = [1,2]
x[1:3] = [2,3]
x[2:] = [3,4,5]


'''
